package com.flashtrack.app.presentation.theme

import androidx.compose.ui.graphics.Color

// ─── Core Backgrounds ────────────────────────────────────────────────────────
val Background       = Color(0xFF0A0A0A)   // Pure near-black page background
val Surface          = Color(0xFF141418)   // Card background (slightly lighter)
val SurfaceVariant   = Color(0xFF1C1C22)   // Elevated cards, input fields
val SurfaceContainer = Color(0xFF0F0F14)   // Inner card rows (Available Balance row)

// ─── Brand / Accent ──────────────────────────────────────────────────────────
val Primary          = Color(0xFF4CAF50)   // Teal-green primary accent
val PrimaryVariant   = Color(0xFF43A047)
val PrimaryContainer = Color(0xFF1B3A1F)   // Debt card background (dark green)

// ─── Transaction Colors ───────────────────────────────────────────────────────
val SpendingRed      = Color(0xFFE53935)   // Expense amounts (red)
val IncomeGreen      = Color(0xFF4CAF50)   // Income amounts (green)
val TransferBlue     = Color(0xFF1E88E5)   // Transfer indicator

// ─── On Colors ───────────────────────────────────────────────────────────────
val OnBackground     = Color(0xFFFFFFFF)
val OnSurface        = Color(0xFFFFFFFF)
val OnSurfaceVariant = Color(0xFF8B8B8B)   // Muted labels
val OnSurfaceDim     = Color(0xFF555560)   // Very muted (placeholders)
val OnPrimary        = Color(0xFFFFFFFF)

// ─── Bottom Navigation ────────────────────────────────────────────────────────
val NavBackground    = Color(0xFF0D0D12)   // Nav bar background
val NavActiveItem    = Color(0xFFFFFFFF)
val NavInactiveItem  = Color(0xFF555566)

// ─── Charts ──────────────────────────────────────────────────────────────────
val ChartSpending    = Color(0xFFE57373)   // Spending line/bars (soft red)
val ChartIncome      = Color(0xFF81C784)   // Income bars
val ChartProjected   = Color(0xFFFFD54F)   // Dashed projected line (yellow)
val ChartGrid        = Color(0xFF1E1E28)   // Chart grid lines

// ─── Status / Semantic ───────────────────────────────────────────────────────
val Success          = Color(0xFF4CAF50)
val Warning          = Color(0xFFFF9800)
val Error            = Color(0xFFE53935)
val GoldAccent       = Color(0xFFFFB300)   // Prediction card tint

// ─── Category Default Colors ─────────────────────────────────────────────────
val CatRed           = Color(0xFFE53935)
val CatOrange        = Color(0xFFFF7043)
val CatAmber         = Color(0xFFFFB300)
val CatGreen         = Color(0xFF4CAF50)
val CatTeal          = Color(0xFF00897B)
val CatBlue          = Color(0xFF1E88E5)
val CatIndigo        = Color(0xFF3949AB)
val CatPurple        = Color(0xFF8E24AA)
val CatPink          = Color(0xFFD81B60)
val CatBrown         = Color(0xFF795548)
val CatGrey          = Color(0xFF757575)

// ─── Misc ────────────────────────────────────────────────────────────────────
val Shimmer1         = Color(0xFF1E1E2A)
val Shimmer2         = Color(0xFF2A2A38)
val FABWhite         = Color(0xFFFFFFFF)
val FABIconBlack     = Color(0xFF000000)
val DividerColor     = Color(0xFF1E1E2A)
val BadgeGreen       = Color(0xFF1B3A1F)
val BadgeGreenText   = Color(0xFF4CAF50)
